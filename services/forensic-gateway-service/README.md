# Forensic Gateway Service

## Status

Planned service root. No implementation exists yet.

This service will become the external API and UI/CLI facade after contracts,
service-local build setup, tests and runtime verification are added in later
slices. It must not contain AST, Joern, BTM, storage, replay or reporting
business logic.
