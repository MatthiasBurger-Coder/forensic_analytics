# Migration Status: forensics_tracing -> forensic_analytics

Date: 2026-05-11

## Completed In forensic_analytics

- Java 25 / JUnit 6 baseline is configured in the Gradle build.
- Engine module is present.
- Domain ingestion models are present.
- Application ingestion use case is present.
- Repository source adapter is present.
- Joern Docker adapter boundary is present.
- CLI `analyze` command is present.
- gRPC ingestion payload descriptors are present in the adapter layer.
- Engine request ingestion is present.
- CLI `ingest-request` command is present.
- Testbed handoff scenario is present.
- Standalone `ingest-request` wiring is covered without requiring an analysis ServiceLoader provider.

## Completed In forensics_tracing

Source inspection of the local `forensics_tracing` checkout found:

- Engine request model and writer: yes.
- Engine request payload kinds aligned by name: yes.
- Gradle mapping fields for `engineRequestEnabled` and `engineRequestFile`: yes.
- Maven mapping fields for `forensics.engineRequestEnabled` and `forensics.engineRequestFile`: yes.
- Legacy local BTM output path retained by the request model: yes by source inspection.

Operational verification is incomplete because the documented root Gradle task `generateBtmRules` was not available in the inspected checkout.

## Verified Cross-Repo

- Request generated by `forensics_tracing`: no.
- Request imported by `forensic_analytics`: no.
- uploadedPayloads: not available.
- Summary path: not available.

Blocker:

```text
Task 'generateBtmRules' not found in root project 'forensics-tracing'.
```

Additional context:

- The inspected `forensics_tracing` checkout was on `main`.
- The inspected `forensics_tracing` checkout had local uncommitted changes.
- No stale or synthetic request file was used as a substitute for real producer output.

## Open

- Expose or document the actual producer command for generating `engine-request.json` in `forensics_tracing`.
- Re-run the cross-repo handoff smoke from a verified producer checkout.
- Direct gRPC client in plugin.
- Persistent Analytics storage beyond in-memory.
- Graph model.
- Replay model.
- Report model.
- LLM context.
- Real Joern image digest decision.
- WildFly performance smoke.

## Final Assessment

Migration is partially complete.

The `forensic_analytics` intake path is implemented and locally verified with fixtures matching the inspected tracing-side request shape, including `RULE_ARTIFACTS` and `DIAGNOSTIC_REPORT` payloads for manifest and checksums. The migration cannot be marked complete until a real `engine-request.json` is generated by `forensics_tracing` and imported by the analytics CLI.
