# Service Communication Matrix

## Status

Communication baseline for the active microservice migration.

This document defines planned communication ownership and sequencing. It does
not define database schemas. Existing logical API contracts define the current
baseline. The current `analysis-store-service` implementation covers only the
`AnalysisJobService` job and artifact metadata subset; the current
`joern-cpg-analysis-service` implementation is a provisional gRPC worker
boundary.

## Contract Locations

| Contract Area | Planned Location |
|---|---|
| gRPC ingestion | `contracts/grpc/forensic-ingestion.proto` |
| analysis job and worker handoff | `contracts/grpc/analysis-job.proto` |
| repository checkout and source snapshot preparation | `contracts/grpc/repository-analysis.proto` |
| build-output package production | planned `contracts/grpc/build-artifact-worker.proto` or an approved owner API contract |
| Java AST source-fact analysis and source-fact byte retrieval | `contracts/grpc/java-ast-analysis.proto` |
| Joern CPG/CFG/DFG semantic artifact analysis | `contracts/grpc/joern-cpg-analysis.proto` |
| BTM generation and BTM owner API | `contracts/grpc/btm-generation.proto` |
| Gateway API | `contracts/openapi/gateway-api.yaml` |
| analysis events | `contracts/events/analysis-events.md` |

These files must remain interface contracts and documentation only. They must
not contain Java implementation code or service-shared runtime classes.

## Matrix

| Producer | Consumer | Protocol | Contract Authority | Purpose | Notes |
|---|---|---|---|---|---|
| `frontend-web-app` | `forensic-gateway-service` | REST/OpenAPI | `contracts/openapi/gateway-api.yaml` | UI reads workspaces, analysis jobs, replay and report views | Frontend must not call internal worker services directly |
| CLI or external client | `forensic-gateway-service` | REST/OpenAPI | `contracts/openapi/gateway-api.yaml` | Public analysis and reporting workflows | Gateway translates public errors without leaking private service details |
| plugin / external client | `forensic-gateway-service` | gRPC/protobuf | Slice 02 public BTM delivery contract | Retrieve completed BTM files or explicit unavailable state | Gateway is a public delivery facade and does not own generated bytes |
| plugin / scanner / runtime collector | `forensic-ingestion-service` | gRPC/protobuf | `contracts/grpc/forensic-ingestion.proto` | Upload repository, scanner and runtime evidence packages | Payload provenance, correlation and schema version must be preserved |
| `forensic-gateway-service` | `forensic-ingestion-service` | REST or gRPC | Deferred outside the repository-to-BTM path unless Slice 02 extends ingestion-oriented submission | Start or observe ingestion-oriented workflows | Gateway is facade only; ingestion owns intake validation |
| `forensic-gateway-service` | `repository-analysis-service` | gRPC | `contracts/grpc/repository-analysis.proto` | Deferred for direct Gateway use; repository-to-BTM submission now enters through Analysis Store orchestration owner API | Gateway must not prepare private workspaces or expose Repository Analysis workspace IDs in public responses |
| `forensic-gateway-service` | `analysis-store-service` | gRPC for implemented job lifecycle and repository-to-BTM orchestration owner API; REST/gRPC for later query views | `contracts/grpc/analysis-job.proto` | Submit public repository-to-BTM requests to the orchestration owner and read canonical readiness/status views | Gateway remains facade-only and must not sequence worker business logic, leak downstream diagnostics or read Analysis Store private DB tables |
| `forensic-gateway-service` | `btm-generation-service` | gRPC/protobuf owner API | Slice 02 public delivery contract plus Slice 10 BTM owner API extension | Retrieve generated BTM bytes or chunks after Analysis Store metadata identifies accepted artifacts | Gateway does not store canonical BTM bytes and must not bypass Analysis Store metadata |
| `forensic-gateway-service` | `graph-replay-service` | REST or gRPC | Slice 15 implementation or explicit deferral decision | Request replay and graph views | Replay responses must expose missing evidence explicitly |
| `forensic-gateway-service` | `report-generation-service` | REST or gRPC | Slice 15 implementation or explicit deferral decision | Request reports and incident packages | Reports must separate evidence, gaps, derived facts and hypotheses |
| `forensic-ingestion-service` | `analysis-store-service` | gRPC or event contract | `contracts/grpc/analysis-job.proto` or `contracts/events/analysis-events.md` | Register accepted raw intake and canonical ingestion state | The current Analysis Store path supports artifact metadata registration, not durable normalized facts |
| `repository-analysis-service` | `analysis-store-service` | gRPC or event contract | `contracts/grpc/repository-analysis.proto`, `contracts/grpc/analysis-job.proto` or `contracts/events/analysis-events.md` | Register source snapshot metadata and checkout diagnostics | Repository service owns workspaces; Analysis Store owns accepted metadata |
| `repository-analysis-service` | `java-ast-analysis-service` | gRPC handoff with job metadata | `contracts/grpc/repository-analysis.proto`, `contracts/grpc/java-ast-analysis.proto`, `contracts/grpc/analysis-job.proto` | Deliver immutable source snapshot identifiers, relative Java roots and bounded source content for AST analysis | Do not share mutable workspace paths as hidden coupling; Slice 12 verifies the handoff completion signal and preserves source-fact byte access metadata |
| `repository-analysis-service` | `build-artifact-worker-service` | planned gRPC handoff or owner API | planned Slice 07 contract | Request a complete build-output package for a commit-pinned source snapshot when no verified Artifact Store/Artifactory or Jenkins artifact is available | Build worker owns produced bytes; Repository Analysis workspace IDs and paths stay private |
| Artifact Store / Artifactory | `repository-analysis-service` or `build-artifact-worker-service` | configured external artifact access | Slice 07 metadata and security policy | Provide an existing versioned complete build-output package when configured | Optional external producer; manifest and checksum mismatch is terminal and must not trigger silent fallback |
| Jenkins | `repository-analysis-service` or `build-artifact-worker-service` | configured external CI API | Slice 07 metadata and security policy | Produce a complete build-output package for the pinned source snapshot when configured | Optional external producer; local quality gates use fakes and do not require Jenkins |
| `repository-analysis-service` | `joern-cpg-analysis-service` | gRPC handoff with job metadata and materialization descriptors | `contracts/grpc/repository-analysis.proto`, `contracts/grpc/joern-cpg-analysis.proto`, `contracts/grpc/analysis-job.proto` | Deliver immutable source snapshot references, source packages or artifact references, complete build-output package references, Joern-local materialization identifiers and relative Java roots for Joern analysis | Joern runtime remains service-contained; Repository Analysis private workspace IDs and mutable workspace paths must not cross the boundary |
| `java-ast-analysis-service` | `analysis-store-service` | gRPC owner API plus job metadata contracts | `contracts/grpc/java-ast-analysis.proto`, `contracts/grpc/analysis-job.proto` or `contracts/events/analysis-events.md` | Submit source-fact artifact references, counts, diagnostics and verified owner retrieval metadata | Analysis Store retrieves source-fact bytes only through `GetSourceFactArtifactBytes` using service-local generated client stubs; static facts must not be treated as runtime execution |
| `joern-cpg-analysis-service` | `analysis-store-service` | gRPC or event contract | `contracts/grpc/joern-cpg-analysis.proto`, `contracts/grpc/analysis-job.proto` or `contracts/events/analysis-events.md` | Submit static semantic artifact references, provenance and mapping diagnostics | The current Analysis Store path accepts job and artifact metadata only; incomplete mappings stay explicit and static semantic output is not runtime execution evidence |
| `analysis-store-service` | `btm-generation-service` | gRPC | `contracts/grpc/btm-generation.proto` and `contracts/grpc/analysis-job.proto` | Provide accepted fact artifact references, semantic artifact references, artifact byte-access metadata and bounded instrumentation target selections for rule generation | Analysis Store owns target-selection metadata; BTM service consumes target snapshots only, must not scan repositories directly and must not treat generated rules as runtime evidence |
| `btm-generation-service` | `analysis-store-service` | gRPC or event contract | `contracts/grpc/btm-generation.proto`, `contracts/grpc/analysis-job.proto` or `contracts/events/analysis-events.md` | Register generated rule artifact metadata, manifests and reproducibility metadata | Rule IDs must be stable and reproducible; metadata registration does not transfer BTM byte ownership |
| `graph-replay-service` | `analysis-store-service` | REST or gRPC | Slice 15 implementation or explicit deferral decision | Read canonical facts, incidents and correlations | Graph/replay must not read private Analysis Store tables |
| `report-generation-service` | `analysis-store-service` | REST or gRPC | Slice 15 implementation or explicit deferral decision | Read evidence, findings, incidents and artifact metadata | Reports must preserve evidence provenance |
| `report-generation-service` | `graph-replay-service` | REST or gRPC | Slice 15 implementation or explicit deferral decision | Read replay and graph context for reports | Graph context remains projection output |
| `report-generation-service` | LLM provider | Provider API | Future provider decision | Optional generated analysis when approved | LLM output is generated analysis, not evidence |

## Error, Retry And Idempotency Rules

The active contract workflow must define concrete error and status models.
Until then:

- public Gateway errors must not leak private payloads, stack traces, secrets or
  source content;
- ingestion uploads require correlation and schema-version preservation;
- worker submissions require idempotency or duplicate-detection semantics;
- long-running jobs require explicit status and retry behavior;
- timeout behavior must be visible to callers;
- failed or partial evidence must be represented as incomplete, rejected,
  unknown or unavailable rather than silently repaired.

## Generated-Code Boundaries

Generated REST or gRPC code may be service-local implementation detail. It must
not become a shared Java DTO or shared Java domain module between services.

If a service generates code from a central contract, the generated output must
be local to that service build or copied through a service-local generation
step. No service may depend on another service's generated classes.

## Contract-Test Expectations

Later contract tests must verify:

- request and response semantics;
- compatibility rules;
- required and optional fields;
- error/status mapping;
- idempotency behavior;
- timeout and retry expectations;
- correlation and provenance preservation;
- generated-code boundary compliance.

The repository contains partial service-local `*ContractTest` classes for
implemented services, but it does not yet provide a dedicated service-level
contract-test Gradle task that covers every contract family. Later contract
slices must add or name an executable contract-test command before claiming
contract readiness.
