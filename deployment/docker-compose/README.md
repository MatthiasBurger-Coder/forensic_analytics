# Local Docker Compose

## Status

Planned local Docker Compose root.

No operational compose file exists yet. Compose material must wait until enough
service-local Dockerfiles and healthcheck behavior are verified for an
end-to-end runtime scenario.

Verified service-local Docker evidence currently exists for
`forensic-ingestion-service`, `analysis-store-service` and
`repository-analysis-service`. This does not claim compose runtime readiness.
