package de.burger.forensics.analytics.services.ingestion.domain;

public enum AnalysisPayloadKind {
    SOURCE_FACTS,
    SEMANTIC_ARTIFACTS,
    RULE_ARTIFACTS,
    RUNTIME_TRACE,
    DIAGNOSTIC_REPORT
}
